/**
 * This Enum will simply change set the value of Eevee's dirction so if for example the player presses 'up'
 * The picture of Eevee dirction will point up will rotate so it points up.
 */
public enum Direction {
    Forward,
    Left,
    Right,
    Back
}
